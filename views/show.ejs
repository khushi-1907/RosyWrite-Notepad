<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Note Detail Page</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    :root {
      --light-blue-bg: #ffedff;
      --dark-blue-bg: #ffbdd6;
      --dark-blue-border: #fdcdfd;
      --input-blue: #ec7fa9;
      --placeholder-blue: #ffedff;
      --text-light: #be6085;
      --button-blue: #be6085;
      --button-blue-hover: #ec7fa9;
    }

    body {
      background: url('/background.webp') no-repeat center center fixed;
      background-size: cover;
    }
  </style>
</head>

<body class="text-[var(--text-light)] min-h-screen font-sans p-8">
  <div class="max-w-3xl mx-auto bg-[var(--light-blue-bg)] bg-opacity-80 rounded-xl p-8 shadow-lg">
    
    <!-- Go Back Button -->
    <a href="/" class="inline-block bg-[var(--button-blue)] hover:bg-[var(--button-blue-hover)] text-white py-2 px-4 mb-8 rounded">
      ← Go Back to Home
    </a>

    <!-- Note Title -->
    <h1 class="text-4xl font-bold mb-4 text-[var(--text-light)]"><%= file %></h1>

    <!-- Note Description -->
    <div class="text-lg leading-relaxed text-[var(--text-light)]">
      <p>
        <%= data %>
      </p>
    </div>
  </div>
</body>

</html>
