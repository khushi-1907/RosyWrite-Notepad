<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Signup - RosyWrite</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="icon" href="/favicon.ico?v=1" type="image/x-icon" />
    <style>
      :root {
        --bg-light: #ffedff;
        --bg-dark: #ffbdd6;
        --border-dark: #fdcdfd;
        --input-bg: #ec93b5;
        --placeholder-color: #ffedff;
        --text-primary: #7a2e4c;
        --button-primary: #d16d87;
        --button-primary-hover: #ec7fa9;
      }
      html, body {
        height: 100%;
        margin: 0;
        background: url("/background.webp") no-repeat center center fixed;
        background-size: cover;
      }
    </style>
  </head>
  <body class="text-[var(--text-primary)] font-sans p-8 min-h-screen">
    <div class="mt-10  max-w-md mx-auto px-6 pt-6 pb-8 bg-white bg-opacity-70 rounded-xl shadow-lg">
      <h1 class="text-3xl font-bold text-center mb-6">Join RosyWrite</h1>
      <form action="/signup" method="POST" class="space-y-4">
        <div>
          <label class="block text-sm font-bold mb-2" for="name">Name</label>
          <input 
            type="text" 
            id="name" 
            name="name" 
            placeholder="Your lovely name" 
            class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-400" 
            required
          />
        </div>
        <div>
          <label class="block text-sm font-bold mb-2" for="email">Email</label>
          <input 
            type="email" 
            id="email" 
            name="email" 
            placeholder="you@example.com" 
            class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-400" 
            required
          />
        </div>
        <div>
          <label class="block text-sm font-bold mb-2" for="password">Password</label>
          <input 
            type="password" 
            id="password" 
            name="password" 
            placeholder="Create a password" 
            class="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-pink-400" 
            required
          />
        </div>
        <button 
          type="submit" 
          class="w-full bg-[var(--button-primary)] hover:bg-[var(--button-primary-hover)] text-white py-3 rounded-md font-bold"
        >
          Blossom In
        </button>
      </form>
      <p class="mt-6 text-center text-sm">
        Already have an account?
        <a href="/login" class="text-pink-600 hover:underline">Sign in here</a>
      </p>
    </div>
  </body>
</html>
